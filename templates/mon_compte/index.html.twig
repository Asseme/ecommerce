{% extends 'base.html.twig' %}

{% block title %}Hello MonCompteController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>
    {{form(registrationForm)}}
    {% if paniers is not empty %}
        <table>
            <thead>
                <th>ID</th>
            </thead>
            <tbody>
                {% for panier in paniers %}
                    <tr>
                        <td><a href="{{path('panier_edit', {'id': panier.id})}}">{{ panier.id }}</a></td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    {% else %}
        <p>Not panier found</p>
    {% endif %}

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ '/Users/assemeregis-cyrillenguessan/Documents/ecommerce/src/Controller/MonCompteController.php'|file_link(0) }}">src/Controller/MonCompteController.php</a></code></li>
        <li>Your template at <code><a href="{{ '/Users/assemeregis-cyrillenguessan/Documents/ecommerce/templates/mon_compte/index.html.twig'|file_link(0) }}">templates/mon_compte/index.html.twig</a></code></li>
    </ul>
</div>
{% endblock %}
